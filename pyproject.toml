[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "userspace-fs"
version = "0.1.0"
description = "Rust-powered user-space filesystem with Python bindings"
requires-python = ">=3.9"

[tool.maturin]
module-name = "userspace_fs"
